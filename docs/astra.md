# i-design：组件体系、业务场景与示例网站实施规划

> 日期：2026-09-13。源码基线：main `62ed38e`。本次实现分支：`feat/astra_redesign`。
> 本文是可拆为 issue/PR 的工程规划；除第 2 节明确列出的本次改动外，所有待办均未在本分支实现。任务优先级、容量和性能预算是建议目标，不代表已测结果或交付承诺。

## 1. 目标与交付边界

让使用者能够完成“发现合适组件 → 理解数据与交互契约 → 调整主题 → 在真实业务页面使用 → 安装到外部项目”的闭环。覆盖对象包括页面布局、表单、表格、各类图标、各类图表、流程图、AI 对话和智能体交互。

采用**共享组件库 + 业务组合层 + 示例应用组 + 全量覆盖实验室**的结构。ERP、OA、分析中心、智能体工作台各自承载自然业务场景；实验室承担少见组件、边界状态和全部主题样式验证。避免为了凑覆盖率将所有组件塞进一个业务页面。

- 范围内：可安装组件、共享逻辑、设计令牌、图标素材与检索、图表与流程能力、页面模板、示例应用、文档、自动生成覆盖报告、测试和版本发布。
- 本轮示例应用阶段范围：可运行前端、确定性 mock 数据、状态迁移、本地持久化、契约适配器与异常注入。真实服务可后接。
- 范围外：完整财务核算/税务合规 ERP、真实企业人事系统、支付、生产审批引擎、自研模型训练、真实外部工具执行、多人实时协作后端。这些需要独立服务与评审，组件库不能承诺具备。
- 保留项目约束：纯色元素填充；图标 + 文字共同表达状态；禁止粗单边线表达类型；`*-solid` 与 `on-*` 配对；图标 SVG/currentColor；共用令牌/图标/纯逻辑；新增库组件按现有多端契约完成适配。
- 本文不假定个人可投入工时。先按依赖与验收拆分，完成首个纵向切片后再用实际耗时估算排期。

## 2. 已核对的现状与本次改版

### 2.1 源码证据与缺口

“源码存在”“端间契约覆盖”“安装可用”“业务成熟”分别核验。`src/data/componentStatus.ts` 明确只按源码判断 ready，不能把 ready 当成生产成熟度。

| 能力       | 基线已有证据                                                                                                                                                                          | 仍需推进的事项                                                                                                                                         |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 多端与主题 | `packages/common`、`packages/vue-next`、`packages/vue`、`packages/react`、移动端/小程序/Flutter；令牌生成脚本与主题配置面板                                                           | 消费端验收矩阵、SSR/打包器测试、版本兼容策略；静态校验不代替各端运行验证                                                                               |
| 原子组件   | `src/components` 包含表单、输入、选择、日期、上传、树、虚拟列表、表格、导航、布局、弹层等                                                                                             | 审核现有 API 后增强；优先补业务组合能力，不重复制造 Input/Table                                                                                        |
| 表格       | `ITable.vue` 已有排序、选择、加载/空态、固定表体高度和虚拟滚动                                                                                                                        | 服务端查询契约、多列/列固定/列设置/编辑/导入导出、树表与批量部分失败                                                                                   |
| 图标       | `packages/common/src/icons/index.ts`，24×24、统一描边；小程序与 Flutter 生成脚本                                                                                                      | 行业分类、中文别名、按需导入、线性/填充变体规划、素材来源和授权追踪                                                                                    |
| 图表       | `IChart*.vue`、`ISparkline.vue`；`logic/chart.ts`、`gantt.ts`、`wordcloud.ts`                                                                                                         | 已有趋势、堆叠、饼环、散点、热力、雷达、漏斗、仪表、箱线、瀑布、桑基、矩形树、甘特、词云、区间缩放；后续补统计口径、缺失值、联动、更多图型及大数据验证 |
| 流程       | `IFlow.vue` 与 `logic/flow.ts`：拖动、缩放、自动布局、框选、分组折叠、对齐、调整尺寸、缩略图、快照；文档演示撤销                                                                      | 节点注册、端口连线编辑与校验、属性检查器、版本化持久化、审批/智能体运行态、拓扑等专用适配                                                              |
| AI 交互    | ChatMessage、PromptInput、ChatSources、ChatThinking、ChatToolCall、ToolChips、ApprovalCard、AgentTasks、ContextCards、InsightCards、DiffTable、AgentScreen、FineTuneCard、ChatList 等 | 统一事件协议、取消/重试/重连、会话分支、引用安全、工具权限、人类确认、产物版本与可观测性；已有 UI 不等于已接模型服务                                   |
| 包构建     | `scripts/build-packages.mjs`、`check-package-exports.mjs`、各包 exports，CI 有 `build:packages`                                                                                       | 干净消费项目安装验收、版本和变更日志、许可证文件、发布演练；未核实 npm 注册表发布状态，不宣称尚无包构建能力                                            |
| 文档与检查 | 生成 inventory/matrix/props；`check:parity`、`test`、`typecheck`、`build`、a11y/responsive/motion；CI 与分支预览                                                                      | API/示例/状态覆盖精确映射、真实应用导航与任务闭环、消费端与性能监控                                                                                    |
| 示例网站   | 基线 `src/router/index.ts` 是文档站路由，React 有演示构建脚本                                                                                                                         | 未见独立 ERP、OA、分析中心或 Agent Studio 业务应用；需新增应用壳和共享 mock 层                                                                         |
| 文档准确性 | README 仍列较早的分类、旧路径说明；当前树未包含 HANDOVER/ROADMAP                                                                                                                      | 文档基于当前源码重建，勿从历史叙述推定已完成状态                                                                                                       |

可复核入口：`src/data/components.ts`、`src/data/componentInventory.ts`、`src/data/componentStatus.ts`、`src/router/index.ts`、`package.json`、`.github/workflows/ci.yml`。目录、导出与覆盖清单继续由脚本生成。

### 2.2 本分支实际交付

- [x] 首页：重新建立标题、留白、主次操作和作品展示层级；采用真实组件场景展示替换铺满首屏的静态主视觉。
- [x] 首页交互：ERP 图表切换；OA 审批状态更新/重置；AI 场景的本地预设问答；数据与演示范围明确标注。
- [x] 组件总览：按中英文名称/用途搜索、分类筛选、匹配数量、无结果恢复、图表/图标/流程/AI 快速入口。
- [x] 公共站点外观：导航标志使用品牌色文字配对、页头节奏、文档示例容器与焦点可见性。
- [x] 本规划：能力地图、可执行任务、示例页面覆盖与验收约束。

这里的首页场景是组件组合预览，不是下文定义的完整业务网站；下文各任务保持未勾选。

## 3. 分层架构与实现顺序

### 3.0 已定：pro 层全端（选项 B）

这是整个规划里最大的前置决策，答案不同 B 组的规模差三到五倍。
**已定为 B：pro 层全端**，与原子组件同样的多端要求，进覆盖矩阵。

`packages/pro-*/` 里是 ProTable / QueryFilter / SchemaForm 这类组合件。
「全端」意味着光 B05（列显隐/排序/宽度/固定/密度/保存视图）就要在
Vue3 / Vue2 / React / 小程序 / Flutter 各做一遍，而这些端对「固定列 +
横向虚拟滚动 + 拖拽改宽」的支持程度差得很远。因此这个决策带三条约束：

1. **先做一个最小 pro 组件的五端垂直切片标定成本**，再排后面的批次。
   切片选 B03 QueryFilter：它是 B 组里最小的 P1，同时已经具备 pro 层的
   全部结构特征——跨端共享的状态机、URL 往返、以及各端差异极大的输入控件。
2. **凡是某端确实做不到的，写成能力声明，不用空壳满足 parity。**
   声明与实现同一次提交，不能等到某个任务卡住了再补。能力差异登记在
   `packages/common/src/contracts/capability.ts`，由 `check:capability` 校验：
   声明里写着「不支持」的组合，那个端就不许存在同名实现文件；
   反过来实现存在却没声明，也算失败。
3. **交互降级要指名替代路径**，而不是「该端暂不支持」一句了事。
   例如小程序没有拖拽改列宽，替代路径是列设置面板里的分档宽度；
   Flutter 没有 URL，替代路径是路由参数对象的同构序列化。

按五端计工作量后，B 组的批次排期随之拉长——这是选 B 的代价，写在这里，
而不是等到某个批次做不完再解释。

```text
现有 packages/common：令牌、图标、数学/格式化/状态逻辑
现有各端包：交互与渲染适配
拟新增业务组合层：ProTable / QueryFilter / SchemaForm / PageState 等
拟新增运行适配层：查询、图表数据、流程存档、AI 事件流
拟新增 examples：ERP / OA / Analytics / Agent Studio / Mobile / Patterns Lab
现有文档站：组件发现、接口、代码、示例入口、覆盖报告
```

建议目录（尚未建立；名称在架构任务完成后固定）：

```text
packages/common/src/contracts/      # 查询、能力声明、图表/流程/AI 契约
packages/common/src/logic/          # 可单测、无框架依赖的 reducer 与校验
packages/common/src/icons/         # 唯一图标源、元数据与生成入口
packages/pro-*/                    # 各端业务组合适配，避免业务逻辑进入原子组件
examples/shared/                   # mock、fixtures、日期/随机种子、权限与持久化
examples/erp/                       # 销售/采购/库存闭环
examples/oa/                        # 审批/组织/协作闭环
examples/analytics/                 # 图表、联动、报告与监控
examples/agent-studio/              # 对话、编排、运行、产物与评测
examples/mobile-workbench/          # 触摸端业务
examples/patterns-lab/              # 注册表自动生成的完整状态与样式覆盖
```

实施原则：先保留现有构建组织方式，通过单个业务切片验证抽象后再拆包；不能一开始迁移全仓。Vue 3 可作为示例应用首个实现，新增**库组件**仍遵守现有多端要求。复杂编辑器在小屏采用列表/属性面板等适配；如果某端确实不支持，必须形成明确能力声明和项目规则修订，不能用空壳满足 parity。

图表保留现有轻量 SVG 为默认路径；高级数据量或复杂图型可评估可选 ECharts 适配包。其 SVG/Canvas 选择取决于数据量和运行环境，必须通过基准测试确定，不将其引入所有基础组件的首屏依赖。[Apache ECharts 渲染器说明](https://echarts.apache.org/handbook/en/best-practices/canvas-vs-svg/)

流程保留当前纯逻辑层与画布，先补端口和校验；只有在正交布线、复杂布局等明确失败用例下再评估独立布局引擎。AI 组件不内置供应商密钥与网络实现，UI 消费统一事件，由应用注入传输适配器。

## 4. 完整能力地图

### 4.1 页面与业务组件

| 层级       | 需覆盖能力                                                                                                        | 主要业务落点                |
| ---------- | ----------------------------------------------------------------------------------------------------------------- | --------------------------- |
| 应用骨架   | 顶部/侧栏/混合导航、折叠菜单、多标签、面包屑、全局搜索、租户切换、通知、用户菜单、帮助、移动抽屉                  | 所有应用                    |
| 页面布局   | 工作台、统计面板、列表、主从、详情、分步表单、左右分栏、三栏编辑、全屏画布、设置、登录、错误页                    | ERP 列表/OA 审批/Agent 编辑 |
| 数据录入   | 基础输入全状态、同步/异步校验、跨字段依赖、动态行、嵌套表单、条件显隐、日期区间、时区、组织/人员/资源选择         | 订单、采购、报销、工具配置  |
| 高级表格   | 服务端查询、排序筛选、分页、选择范围、列设置、固定列/表头、多级表头、合并单元格、树表、行详情、编辑、汇总、虚拟化 | 订单/库存/审计/评测         |
| 查询与操作 | 快速/高级筛选、筛选标签、保存视图、批量操作、导入映射/预校验、导出任务、长任务取消、部分失败重试                  | ERP/Analytics               |
| 内容与协作 | Markdown、安全富文本、代码、评论/回复/@、附件预览、版本差异、活动记录、知识树、全文检索                           | OA/Agent                    |
| 时间与项目 | 日历/月周日议程、排班、会议预订、拖动看板、甘特依赖、里程碑、时间轴                                               | OA 项目中心                 |
| 状态与权限 | 加载/空/搜索无果/失败/离线/无权限/部分成功/过期/冲突；页面、区块、字段、行、操作权限                              | 全部                        |
| 运维与设置 | 用户/角色/组织、审计、消息偏好、集成配置、数据源、Webhook 状态、任务日志                                          | OA 管理/Agent 设置          |
| 内容型页面 | Landing/Hero、功能展示、定价样式、FAQ、帮助中心、更新日志、资源库                                                 | Patterns Lab 模板页         |
| 移动特性   | 下拉刷新、触底加载、滑动操作、底部弹层、安全区、键盘避让、拍照/附件适配、断网恢复                                 | Mobile Workbench            |

### 4.2 图标体系

按语义域建立分类：通用操作、方向导航、状态反馈、用户/组织/权限、文件格式、表格/图表、流程节点/边、AI/工具/模型、设备/网络/云、采购/销售/库存/物流、财务/合同、日历/会议/协作、媒体/编辑器、地图/位置、开发/数据、安全/审计、移动手势、品牌/技术栈。

统一元数据：稳定 ID、中文名、英文名、别名、标签、分类、变体、viewBox、stroke、设计尺寸、版本、弃用替代项、来源与许可证。已有 path 格式保持兼容；填充/双色如果需要，先扩展规范和生成器，再添加素材，不能把 fill 语义硬塞入描边 path。

不以任意图标数量作为完成标准。以示例页面所需语义、可发现性和真实导出覆盖为准；禁止 emoji 代替图标，插画与品牌标志单列，避免授权混用。

### 4.3 图表体系

| 图表族     | 基线已见组件                             | 需补充/深化的形态与能力                                                                            | 覆盖应用           |
| ---------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------ |
| 指标与趋势 | Statistic、Sparkline、折线、面积、柱状   | 指标卡语义、同比/环比/目标、横向条形、组合/双轴、阶梯、范围带、时间断点、预测区间                  | ERP/Analytics      |
| 对比与排名 | 分组/堆叠柱状、雷达                      | 百分比堆叠、哑铃、坡度、子弹图、棒棒糖、排名变化                                                   | Analytics          |
| 构成与层级 | 饼/环、矩形树                            | 半环、多环使用边界、旭日、Icicle、层级下钻、极小分类合并                                           | ERP/Analytics      |
| 分布与相关 | 散点/气泡、箱线、热力                    | 直方图分箱、密度、误差线、小提琴、散点矩阵、相关矩阵、回归/参考线                                  | Analytics          |
| 转化与归因 | 漏斗、瀑布、桑基                         | 漏斗转化口径、留存 cohort、帕累托、增减贡献、流量平衡校验                                          | Analytics          |
| 时序与状态 | 甘特、仪表、区间缩放                     | 状态时间线、日历热力、事件注记、实时滑窗、数据延迟/暂停、断流标注                                  | OA/Analytics       |
| 关系与网络 | Flow 画布可表达节点边                    | 树/组织图、关系网络、DAG、服务拓扑、调用轨迹、弦图、邻接矩阵                                       | OA/Agent/Analytics |
| 地理空间   | 未见独立地理组件                         | 点/分级设色/聚合/轨迹/流向、GeoJSON、投影、坐标与地图来源说明                                      | Analytics 扩展     |
| 文本与业务 | 词云、InsightCards                       | 词频排序替代视图、可访问文本摘要、财务 K 线/OHLC（业务有需求再做）                                 | Analytics 扩展     |
| 全图通用   | 图例、部分组件数据表/提示、分类/色阶令牌 | 空/缺失/非法数据、加载、统一单位与时区、键盘选择、联动刷选、下钻返回、导出、可读数据表、截图确定性 | 全部图表           |

图型扩展要有“回答什么问题、什么时候不用、最小数据契约、单位与聚合方式”。图表缺失值不可默认为零；百分比分母为零显示不可计算；类别色不因排序而换身份；柱状图不截断基线。不能用设计稿上的百分比反推不存在的业务数据。

### 4.4 流程与 AI 体系

流程需覆盖：审批链、条件分支、并行/汇合、循环/子流程、状态机、组织树、DAG、拓扑、数据血缘、智能体编排；配套节点库、搜索、画布、端口、连线、工具栏、检查器、大纲、缩略图、问题列表、版本对比、运行轨迹。

AI 需覆盖：新建/搜索/归档会话、消息组/分支、Markdown/代码/表格/附件/引用、语音输入适配、流式生成、取消/重试/断线恢复、工具调用、公开执行摘要、结构化输出、候选建议、上下文管理、权限确认、任务/运行列表、模型/工具配置、产物预览/差异/导出、评测与反馈。执行摘要只展示系统提供的可公开状态与依据，不承诺展示模型内部推理。

## 5. 工单拆分规则

优先级：P0 ＝可信接入与基础约束；P1 ＝首个可用业务闭环；P2 ＝广覆盖与高级交互；P3 ＝专门场景。大小 S/M/L 是**相对复杂度**，L 必须在开发前拆成若干可验收 PR，不换算为未经验证的人天。各表“依赖”均指本文任务 ID。

每个任务关闭都应包含：源码/API、文档示例、必要的真实交互测试、明暗/窄屏/减少动效、端适配及生成物更新。任务标记完成需附 PR 和验证结果；不能因为功能名称在目录中出现就勾选。

### A. 基础治理与可消费交付

| ID  | 优先级/大小 | 任务与产物                                                                           | 依赖    | 单项验收                                                                    |
| --- | ----------- | ------------------------------------------------------------------------------------ | ------- | --------------------------------------------------------------------------- |
| A01 | P0/M        | 生成 capability registry：来源路径、导出、端覆盖、成熟度、状态集、文档路由、示例用例 | 无      | 删除一个组件/导出后检查明确失败；不手写来源文件列表                         |
| A02 | P0/M        | 外部安装烟测：pack tarball → 干净 Vue3/React/Vue2/移动消费项目，验证 CSS/类型/peer   | 无      | 不使用仓库 alias；import、require（支持目标）、类型提示、打包与实际渲染通过 |
| A03 | P0/M        | 版本/变更日志/弃用/兼容策略，发布 dry-run 与预发布通道                               | A02     | 同版本包可安装、破坏性变更有迁移文档；发布密钥只在 CI，发布仍独立授权       |
| A04 | P0/S        | 校准 README/安装/目录/支持范围，新增贡献指南和真实验证记录                           | A01,A02 | 文档命令在干净 checkout 可复现；不把源码 ready 写成生产已支持               |
| A05 | P0/M        | 检查 Token 使用、颜色配对、SVG 元数据、状态文字与硬编码违规                          | A01     | 对禁止渐变填充/粗状态边线/硬编码白字的反例检查失败；允许背景纹理            |
| A06 | P0/M        | 兼容与 SSR 评估：无 DOM 导入、Hydration、CSP、主题初始状态                           | A02     | 明确测试矩阵；运行期专用 API 不在模块顶层访问；不声称未测试端支持           |
| A07 | P1/M        | 独立包按需体积报告与依赖关系检查                                                     | A02     | 单按钮入口不携带流程/图表/三维资源；以实际 gzip 与依赖图记录变化            |
| A08 | P0/S        | 素材/依赖 license 文件与来源清单                                                     | 无      | 分发包包含许可证；第三方图标/地图/字体有来源和授权记录                      |

### B. 原子组件补强与页面组合

| ID  | 优先级/大小 | 任务与产物                                                   | 依赖    | 单项验收                                                            |
| --- | ----------- | ------------------------------------------------------------ | ------- | ------------------------------------------------------------------- |
| B01 | P1/M        | AppShell：导航、面包屑、用户菜单、移动抽屉、页面操作槽       | A01,A05 | 刷新深链恢复当前导航；320px 无整页溢出；键盘关闭抽屉回原焦点        |
| B02 | P1/M        | PageState：加载/空/无权限/失败/离线/部分成功/过期统一组合    | A01     | 各状态有原因与可执行恢复动作，局部失败不清空已成功数据              |
| B03 | P1/M        | QueryFilter：字段条件、快捷筛选、清除、折叠、URL 同步        | B02     | 筛选变更回第一页；返回页面恢复条件；无效 URL 参数回退并提示         |
| B04 | P1/L        | ProTable 查询层：分页、排序、筛选、请求取消、过期响应丢弃    | B03,A01 | 快速连续查询不被旧响应覆盖；分页总数与结果同请求口径                |
| B05 | P1/L        | ProTable 列能力：列显隐/排序/宽度/固定、密度、保存视图       | B04     | 列设置持久化可重置；固定列在窄屏局部滚动；隐藏列不会遗漏权限校验    |
| B06 | P2/L        | 树表/行展开/多级表头/合并/分组汇总/虚拟化组合                | B04     | 稳定 rowKey；排序和展开不丢选择；可变行高场景有明确策略             |
| B07 | P1/M        | 批量操作栏：当前页/跨页/全部匹配范围、选择摘要、部分失败     | B04     | 明确选择作用域；失败行可定位；成功项不重复提交                      |
| B08 | P1/L        | SchemaForm：字段注册、显隐/依赖/跨字段/异步校验、数组子表    | A01,B02 | 不执行 schema 内字符串代码；依赖环报错；服务端错误定位字段          |
| B09 | P1/M        | FormPage/DrawerForm/ModalForm/StepForm，草稿和离开保护       | B08     | 重复提交拦截；失败保留输入；返回上一步不丢数据；重置有明确范围      |
| B10 | P1/M        | 人员/组织/资源选择：远程检索、已选回显、禁用原因             | B08     | 分页选择不丢失；无权限项不可选；停用实体仍可读历史记录              |
| B11 | P2/L        | ImportWizard/ExportJob：上传、列映射、预校验、错误下载、取消 | B04,B08 | 预校验不写业务数据；列映射可回退；部分失败可重试且幂等              |
| B12 | P1/M        | DetailPage：摘要、字段组、关联列表、状态操作、时间轴         | B01,B02 | 列表到详情再返回保留筛选/滚动；失效记录展示明确状态                 |
| B13 | P1/M        | Markdown/富文本只读与编辑适配、代码/附件预览                 | A08,B02 | HTML 清洗、危险 URL 拒绝；只读内容与工具栏权限一致                  |
| B14 | P2/M        | 评论线程、@、活动记录、未读定位                              | B10,B13 | 删除父评论保留回复上下文；编辑版本和时间可见；失败可恢复            |
| B15 | P2/L        | 日历/排期/看板：拖动、日期范围、人员泳道、键盘替代动作       | B01,B10 | 拖动和菜单移动结果一致；时区/跨日冲突明确；不可拖项说明原因         |
| B16 | P1/M        | 登录/设置/用户/角色/组织管理页面模式                         | B01,B09 | mock 明确提示；角色切换同时影响菜单、字段和动作；前端不代替服务鉴权 |
| B17 | P2/M        | 公共网页模式：Hero、功能区、定价样式、FAQ、帮助、更新日志    | B01,A05 | 有真实导航/展开操作；价格仅为标注的示例，不连接真实交易             |
| B18 | P1/M        | 通知中心/全局命令/异步任务中心整合                           | B01,B02 | 任务可追踪、通知可定位业务对象、命令受当前权限约束                  |

### C. 图标与资源

| ID  | 优先级/大小 | 任务与产物                                               | 依赖    | 单项验收                                                          |
| --- | ----------- | -------------------------------------------------------- | ------- | ----------------------------------------------------------------- |
| C01 | P1/M        | 图标元数据/分类/中英文别名、缺失清单                     | A01,A08 | 业务名称能检索到图标；重复 ID、无路径、缺来源检测失败             |
| C02 | P1/M        | 通用/业务/图表/流程/AI 图标族扩充                        | C01     | 按 4.2 类别和应用语义缺口逐批交付；线宽/网格/端点一致，低尺寸可辨 |
| C03 | P2/M        | 描边/填充变体协议与生成器兼容                            | C01     | 旧 IconName 调用不破坏；每端渲染语义一致；双色有对比度和单色降级  |
| C04 | P1/M        | 图标浏览器：分类、颜色/尺寸/线宽预览、SVG/代码复制、下载 | C01,C02 | 复制与文件实际可用；键盘浏览、无结果恢复、主题变化同步            |
| C05 | P1/M        | 单图标按需导出、类型生成、重复形状检查                   | C01,A07 | tree-shaking 实测单图标不拉全量；生成物改动可复现                 |
| C06 | P2/M        | 小白/十五插画及异常/空态资源矩阵、许可与用法             | A08,B02 | WebP srcset/暗色与替代文本齐全；SVG 图标和位图插画分类明确        |

### D. 图表与分析能力

| ID  | 优先级/大小 | 任务与产物                                                      | 依赖            | 单项验收                                                          |
| --- | ----------- | --------------------------------------------------------------- | --------------- | ----------------------------------------------------------------- |
| D01 | P1/M        | ChartDataset/ChartSpec、单位/时区/聚合/数据质量语义             | A01             | 单点/全零/负数/null/NaN/缺维度有定义；已有 API 通过适配保持兼容   |
| D02 | P1/M        | ChartFrame：标题、副标题、口径、图例、空错加载、数据表/下载     | D01,B02         | 全图型有文本和表格出口；数据表与图值同源；缺失值不补零            |
| D03 | P1/M        | 趋势/对比增强：百分比堆叠、阶梯、目标标注、范围带               | D01,D02         | 有可核算 fixtures；百分比分母验证（零总量与负值不换算并说明）     |
| D13 | P1/M        | 横条与双轴：轴系重构（横向坐标轴、第二值轴与单位限制）         | D03             | 双轴明确单位与适用限制；横条的标签与网格与纵向一致                |
| D04 | P2/L        | 分布/统计：直方、密度、误差、小提琴、散点矩阵、分位数配置       | D01,D02         | 公式/分箱规则在共享逻辑；空样本/极端值/样本量有提示               |
| D05 | P2/L        | 构成/归因：旭日、Icicle、帕累托、留存、贡献/流量平衡            | D01,D02         | 父子汇总、流入流出、留存分母及未知样本可追溯                      |
| D06 | P1/M        | 联动控制器：过滤/刷选/下钻/返回/图表到明细                      | D01,B03         | 统一 SelectionEvent；循环联动终止；返回恢复上一级筛选             |
| D07 | P2/M        | 实时滑窗、事件标记、数据延迟、暂停恢复和断流状态                | D01,D02         | 限制缓存长度；乱序事件处理；断流不显示“0”；暂停期间状态可见       |
| D08 | P2/L        | 图关系适配：组织图/树/DAG/网络、调用轨迹、邻接矩阵              | D01,E02         | 关系类型有线型/箭头/标签；跨图高亮不改变原数据                    |
| D09 | P3/L        | 地理可选包：GeoJSON/点位/分级设色/轨迹/流向                     | D01,D02,A08     | 投影/坐标/素材来源明示；无地图权限或无坐标有替代视图              |
| D10 | P2/M        | 图表导出：SVG/PNG/CSV，标题/单位/口径/时间附带                  | D02             | 导出不只截当前视口；字体/颜色可读；CSV 处理公式注入与编码         |
| D11 | P2/L        | SVG 与可选渲染适配器性能基线、降采样/Worker 边界                | D01,A07         | 固定设备/数据/分辨率记录首绘与交互；降采样保留极值且可回看原数据  |
| D12 | P2/M        | 图表选择指南和全图型交互 fixtures                               | D03,D04,D05,D06 | 每种图有业务问题、反例、schema、状态与示例页面链接                |

### E. 流程与可视化编辑

| ID  | 优先级/大小 | 任务与产物                                           | 依赖        | 单项验收                                                            |
| --- | ----------- | ---------------------------------------------------- | ----------- | ------------------------------------------------------------------- |
| E01 | P1/M        | GraphDocument：版本、节点/端口/边/分组/视口/元数据   | A01         | JSON 往返无损；版本迁移测试；不可识别节点显示只读占位和原因         |
| E02 | P1/L        | NodeRegistry/节点工具箱/属性检查器                   | E01,B08,C01 | schema 驱动属性；不同业务节点插入无需修改画布核心；删除同步边       |
| E03 | P1/L        | 端口连线编辑/断开/重连/连线类型/校验面板             | E02         | 自环、悬空、重复边、方向/类型不匹配有可定位错误；环按图类型规则处理 |
| E04 | P1/M        | 命令式编辑事务、撤销/重做、复制粘贴/删除、快捷键     | E02,E03     | 一次拖动一条历史；粘贴重映射 ID；输入框内快捷键不误删节点           |
| E05 | P2/M        | 布局与导航：泳道、子流程、自动布局策略、搜索定位     | E03,E04     | 分组/端口/人工位置保留策略明确；布局结果可撤销；孤立节点可定位      |
| E06 | P1/M        | GraphStore：自动保存、草稿恢复、版本冲突与校验后发布 | E01,E04     | 409 冲突不覆盖他人内容；保存失败有重试；只读模式不产生编辑命令      |
| E07 | P1/L        | 审批/Agent 节点模板：条件、并行汇合、等待确认、结束  | E03,E06     | 分支条件完整性与必填属性验证；区分“定义合法”和“运行成功”            |
| E08 | P1/M        | 运行态覆盖：节点/边状态、时间线、输入输出、失败定位  | E07,F01     | 运行引用固定流程版本；失败重试不会直接改定义；状态含文字与时间      |
| E09 | P2/M        | 状态机/拓扑/血缘模板与差异查看                       | E05,E06,D08 | 场景使用独立规则；能区分调用/关联/依赖；增删改有可访问标签          |
| E10 | P2/M        | 大图/触摸/键盘/导出验收                              | E04,E05,E08 | 性能用例含大图；键盘可定位与编辑；手机有结构列表与属性面板          |

### F. AI 对话与智能体

| ID  | 优先级/大小 | 任务与产物                                                                  | 依赖        | 单项验收                                                                         |
| --- | ----------- | --------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------- |
| F01 | P0/M        | Conversation/MessagePart/RunEvent/ToolCall/Artifact/Approval 契约与 reducer | A01         | 事件重放确定；乱序/重复/迟到/终态后事件处理明确；UI 不直接解析供应商字段         |
| F02 | P1/M        | 传输适配：mock + SSE/fetch 流；取消、重试、恢复游标                         | F01         | 取消后晚到片段不续写；同幂等键不重复执行；断线恢复不会重复 token                 |
| F03 | P1/M        | 会话管理：列表、搜索、重命名、归档、删除恢复、分页                          | F01,B02     | 选中会话刷新可恢复；长列表不跳动；无权限/失效会话显示原因                        |
| F04 | P1/L        | 消息渲染：Markdown/代码/表格/附件/引用、多段混排和流式更新                  | F01,B13     | 危险 HTML/链接被拒绝；引用可定位来源；大代码块不阻塞输入                         |
| F05 | P1/M        | 输入台增强：@上下文、/命令、附件校验、粘贴、IME、草稿                       | F01,B10     | 中文输入法回车不误发；超限/重复附件有反馈；草稿按会话隔离                        |
| F06 | P1/M        | 生成生命周期：排队/连接/生成/停止/失败/重试/完成                            | F02,F04     | 切换会话不串流；卸载清理连接；长内容滚动尊重用户已上翻位置                       |
| F07 | P1/M        | 工具调用 UI：输入输出、并行、重试、耗时、错误与脱敏                         | F01,F02     | 日志不包含 secret；失败保留上下文；折叠摘要与详情状态一致                        |
| F08 | P1/L        | ActionApproval：动作预览、权限/过期/版本/单次决定/拒绝/取消                 | F01,F07,B12 | 确认绑定 runId、操作和版本；重复点击幂等；过期重新确认；前端确认不绕过服务端鉴权 |
| F09 | P2/M        | 会话编辑/重新生成/分支比较                                                  | F03,F06     | 分支引用 parentId；重生成不悄悄覆盖旧产物；切换分支保持上下文                    |
| F10 | P1/M        | 产物工作区：文档/表格/代码/图表预览、版本、差异、采纳/导出                  | F04,F08,D02 | 预览隔离；生成内容不能直接执行宿主脚本；可逐项采纳并撤销                         |
| F11 | P2/M        | 上下文管理：来源选择、引用权限、截断/失效提示、容量预算                     | F05,F04     | 显示实际可用来源；失效引用不会继续伪装有效；预算来源注明实际/估计                |
| F12 | P2/L        | Agent 配置：模型能力、工具 schema、变量、知识库、测试输入                   | F01,E07,B08 | 配置校验和发布分离；秘密值不进入前端持久化/示例导出                              |
| F13 | P2/M        | RunExplorer：节点轨迹、调用列表、用量、首字/总耗时、错误分组                | F07,E08     | 所有面板共享 runId/traceId；缺失用量显示未知；不凭 token 猜账单                  |
| F14 | P2/M        | 评测/反馈：数据集、执行结果、人工评分、回归比较                             | F13,B04     | prompt/model/version/fixture 固定；成功定义可复现；人工与自动指标分开            |
| F15 | P3/M        | 语音/图像等多模态接入槽、能力探测与降级                                     | F05,F06     | 权限拒绝可回到文本；取消及时释放资源；没有真实接入就标明演示                     |

## 6. 示例网站组：页面、流程与覆盖关系

业务示例统一用虚构组织与确定性数据，不放真实客户信息。应用共用导航/主题/权限/数据服务，允许差异化密度和布局。以下路线均为计划路径，不是当前已部署链接。

| 应用             | 核心页面/路由                                                                                              | 应闭合的用户任务                                                                               | 主要样式与组件                                                                            |
| ---------------- | ---------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| ERP Workspace    | `/erp/dashboard`、`orders`、`orders/:id`、`orders/new`、`purchase`、`inventory`、`suppliers`、`imports`    | 查库存 → 新建采购申请 → 审批 → 入库 → 销售出库 → 库存/经营看板同步；取消/退回有分支            | AppShell、QueryFilter、ProTable、动态明细表单、详情、选择器、批量、导入、图表、状态时间轴 |
| OA Workspace     | `/oa/home`、`approvals`、`approvals/:id`、`forms/new`、`calendar`、`projects`、`knowledge`、`organization` | 提交报销/请假 → 待办 → 同意/退回 → 通知；会议创建 → 冲突 → 改期；任务跨看板/甘特同步           | 人员组织选择、StepForm、审批链、评论、附件、日历、看板、甘特、知识编辑                    |
| Analytics Studio | `/analytics/overview`、`explore`、`dashboard/:id`、`realtime`、`reports`、`geo`                            | 选指标/维度/周期 → 看图 → 刷选 → 明细 → 保存视图 → 导出；断流/空样本可恢复                     | 全图表族、联动、筛选、数据表、透视模式、指标卡、实时/地图选配                             |
| Agent Studio     | `/agents`、`agents/:id/edit`、`chat/:id`、`runs/:id`、`artifacts/:id`、`evaluations`、`tools`              | 选助手 → 提问/引用 → 工具调用 → 确认动作 → 执行 → 查看产物/差异 → 回溯运行；失败/取消/拒绝分支 | AI 全组件、三栏工作台、流程画布、工具表单、审批卡、产物面板、运行追踪                     |
| Mobile Workbench | `/mobile/home`、`tasks`、`approval/:id`、`chat`、`profile`                                                 | 从待办处理同一 OA 单据 → 回到列表状态更新；断网缓存待处理 → 联网明确重新提交                   | NavBar、Tabbar、Cell、SwipeCell、Popup、Picker、ActionSheet、刷新/分页、移动对话          |
| Patterns Lab     | `/lab/components`、`states`、`themes`、`charts`、`flows`、`templates`                                      | 查组件 → 切换状态/主题/密度/端 → 对照差异 → 复制源码 → 定位业务场景                            | 全量 registry 自动生成；补齐少见图表、错误态、主题样式、Landing/FAQ/帮助等                |

**ERP 与 OA 不重复实现基础能力**：它们共享人员、组织、审批记录与附件实体。Agent 可以读同一份销售示例数据，提出采购建议并通过同一审批适配器；这让跨站示例证明组件复用，同时保持应用各自可独立启动。

共享到这个程度，ERP 与 OA 更接近「一个应用的两个模块」而不是两个应用。
落地顺序据此定：**先把这两个模块合成一个应用跑通，再按需要拆成两个入口**。
六个示例应用每个都要确定性 mock、状态机、本地持久化与异常注入，
一开始就立六个壳，最可能的结果是六个都停在壳的状态、共享实体各写一份。
拆分的时机是共享层已经被两个模块真实复用过，而不是排期表的第一天。

### 6.1 应用任务

| ID  | 优先级/大小 | 任务与产物                                                          | 依赖                    | 单项验收                                                        |
| --- | ----------- | ------------------------------------------------------------------- | ----------------------- | --------------------------------------------------------------- |
| G01 | P1/M        | examples/shared：固定随机种子、时钟、mock API、版本化本地存储、重置 | A01,F01                 | 刷新恢复；一键重置；相同 seed 生成相同图表/实体；无随机截图漂移 |
| G02 | P1/M        | 示例门户、共用应用壳、应用间链接、示例源码入口                      | B01,G01                 | 每个应用独立启动/子路径构建；文档与示例双向链接无死路           |
| G03 | P1/L        | ERP 第一切片：订单列表 → 新建 → 详情 → 状态更新                     | G02,B04,B07,B09,B12     | 成功/校验失败/403/409/重复提交都有 fixture；列表与详情同步      |
| G04 | P2/L        | ERP 采购/库存/供应商/导入/经营分析                                  | G03,B11,D06             | 入库出库改变同一库存流水；汇总与明细同源；部分导入失败可追踪    |
| G05 | P1/L        | OA 第一切片：提交申请 → 待办审批 → 退回再提交 → 通知                | G02,B09,B10,B12,B18,E07 | 角色与状态矩阵完整；申请人不能审批自己的受限单据                |
| G06 | P2/L        | OA 项目/看板/日历/知识库/组织管理                                   | G05,B13,B14,B15,B16     | 拖动后时间/状态一致；会议冲突可恢复；知识搜索定位具体内容       |
| G07 | P2/L        | Analytics：图表探索、联动、报告、实时监控                           | G02,D06,D07,D10         | 图表/明细/导出一致；断流明确标注；图表族逐项绑定用例            |
| G08 | P1/L        | Agent 第一切片：销售问题 → 工具结果 → 人类确认 → 产物               | G02,F03,F06,F07,F08,F10 | 取消/失败/拒绝/成功均可重放；展示执行依据与确认对象             |
| G09 | P2/L        | Agent 编排/运行详情/工具管理/评测                                   | G08,E08,F12,F13,F14     | 定义版本和运行版本一致；发布前错误阻断；评测差异可追溯          |
| G10 | P2/L        | Mobile Workbench 与共享单据联动                                     | G05,F06                 | 触摸/键盘/安全区与离线分支实测；使用移动端包而非缩小桌面页面    |
| G11 | P0/M        | Patterns Lab 自动注册与主题/状态 playground                         | A01,G02                 | 每个可用组件都有至少一个可运行演示；只存在名字/截图不能计覆盖   |
| G12 | P2/M        | 页面模板导出与外部项目运行                                          | G03,G05,G08,G11,A02     | 导出不依赖仓库路径；mock/真实适配入口分开；缺环境变量能说明     |

### 6.2 “全部样式”如何定义和验收

样式集合来自组件的公共 props/variants、主题令牌和状态模型，不能靠人工随手挑几张截图定义“全覆盖”。建立生成数据：

```ts
interface CoverageCase {
  capabilityId: string;
  variant: string;
  state: string;
  platform: string;
  theme: "light" | "dark";
  density: "comfortable" | "compact";
  exampleRoute: string;
  fixtureId: string;
  testId: string;
  availability: "implemented" | "planned" | "unsupported";
  reason?: string;
}
```

覆盖口径：

1. **能力覆盖率**＝存在可运行案例的 implemented 能力 / registry 中 implemented 能力。目标 100%。计划中项单列，不能藏在分母外让读者误以为全量已实现。
2. **状态覆盖率**＝已验证必需状态组合 / 该能力声明的必需状态组合。所有组件覆盖默认/禁用/焦点，异步组件增加 loading/empty/error，动作组件增加确认/冲突/取消。
3. **业务覆盖率**＝被业务任务实际使用的业务能力 / 本里程碑业务能力范围。冷门图型可由 Lab 覆盖，但要明确标注 Lab-only。
4. **端覆盖率**沿用现有矩阵，增加 runtime-verified 与 source-only 区别，避免静态齐全替代真机运行。
5. 主题组合用 pairwise 控制常规回归规模；品牌主按钮、状态标签、图表色阶、确认/危险操作在明暗主题与浅品牌色下逐项全测。

| 验证维度  | 最小集合                                                             | 执行位置                     |
| --------- | -------------------------------------------------------------------- | ---------------------------- |
| 主题      | 亮/暗、默认品牌、浅品牌/高亮色压力样本                               | Lab + 核心业务页             |
| 密度/尺寸 | 舒适/紧凑、组件声明的 size、长中英文/数字                            | Lab + ERP 列表               |
| 视口      | 320、390、768、1440；宽屏分析/画布按需要补                           | Lab + 各应用核心流程         |
| 输入      | 鼠标、键盘、触摸、中文输入法                                         | 表单/菜单/对话/画布          |
| 内容      | 空/单条/大量、长文本/缺字段/非法值/超限                              | fixtures 自动注入            |
| 状态      | 默认、悬停、焦点、选择、禁用、加载、失败、成功、部分成功、过期、取消 | 由能力元数据生成             |
| 权限      | 访客、只读、编辑、审批、管理员；字段/行权限                          | mock 权限策略 + API 契约测试 |
| 动效      | 系统减少动效、站内关闭、正常模式                                     | 既有 motion 检查 + 新交互    |

## 7. 数据模型、状态迁移与接口契约

本节是目标设计，不是当前接口已实现声明。新增契约避免替换现有 props，优先写 adapter。

### 7.1 共用实体

- Organization → User/Membership → Role/Permission；每个记录带 `tenantId`，作用域隔离由服务端落实。
- Product → Warehouse → StockMovement；库存由入/出库流水推导，不能让页面各写一份库存数。
- SalesOrder/PurchaseOrder → OrderLine → Product；金额采用最小货币单位整数和币种，显示格式与计算分离。
- ApprovalInstance → ApprovalTask → ApprovalDecision；关联业务对象、定义版本和审计记录。
- Conversation → Message → MessagePart；Message 支持 parentId/branchId，来源引用关联 SourceDocument/Chunk。
- AgentDefinition → AgentVersion → Run → RunEvent/ToolCall/ArtifactVersion；运行固定版本，工具确认绑定调用与内容摘要。
- Dashboard → Widget → Dataset/QuerySpec；统一时区/单位/聚合与数据更新时间，避免看板卡片统计周期不一致。
- Attachment/Notification/AuditEvent 为共用实体；AuditEvent 只追加，包含 actor/action/object/version/result/time/traceId，敏感字段脱敏。

所有可变实体包含 `id/version/createdAt/updatedAt`；示例约定 UTC ISO 时间存储、界面按明确时区显示。权限矩阵、日期格式、金额格式放共享层；页面不复制规则。

### 7.2 状态机与异常

| 对象     | 主状态                                                                                    | 关键约束/异常                                                       |
| -------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| 表单     | pristine → editing → validating → submitting → success/error                              | 失败保留输入；取消只取消当前请求；离开草稿可恢复                    |
| 订单     | draft → pending → approved → fulfilling → completed                                       | pending 可撤回；rejected 回 draft；完成后通过退货单处理，不篡改历史 |
| 审批     | pending → approved/rejected/canceled/expired                                              | 决定绑定 taskVersion；一次有效决定；403/409/410 有明确恢复提示      |
| 数据请求 | idle → loading → ready/empty/error/stale                                                  | 缓存数据刷新失败显示 stale；不同 queryKey 不能共享旧响应            |
| 流程定义 | draft → validating → published → archived                                                 | 发布版本不可直接改写；运行绑定 published version                    |
| AI Run   | queued → connecting → streaming/tool-running/waiting-approval → completed/failed/canceled | cancel/terminal 后不接受正文追加；多工具状态在 ToolCall 单独追踪    |
| ToolCall | pending → running → succeeded/failed；或 pending → awaiting-approval → rejected/expired   | 取消运行不等于外部动作已撤销；适配器必须报告真实结果或未知          |
| 导出     | queued → running → ready/failed/canceled/expired                                          | 下载失效可重新生成；取消幂等；不保留无授权数据文件                  |

### 7.3 API 边界示例

```ts
type PageQuery = {
  cursor?: string;
  pageSize: number;
  sort: { field: string; direction: "asc" | "desc" }[];
  filters: { field: string; operator: string; value: unknown }[];
};
type PageResult<T> = {
  items: T[];
  nextCursor?: string;
  total?: number;
  queryKey: string;
  asOf: string;
  traceId: string;
};
type ApiError = {
  code: string;
  message: string;
  retryable: boolean;
  fieldErrors?: Record<string, string>;
  traceId: string;
};
type RunEvent = {
  eventId: string;
  runId: string;
  sequence: number;
  at: string;
  type:
    | "message.delta"
    | "tool.started"
    | "tool.completed"
    | "approval.required"
    | "artifact.updated"
    | "run.completed"
    | "run.failed"
    | "run.canceled";
  payload: unknown; // 落地时换成各 type 对应的判别联合，禁止 UI 盲读 unknown
};
interface RunTransport {
  start(
    input: unknown,
    options: { signal: AbortSignal; idempotencyKey: string }
  ): AsyncIterable<RunEvent>;
  resume(
    runId: string,
    afterSequence: number,
    signal: AbortSignal
  ): AsyncIterable<RunEvent>;
  cancel(runId: string): Promise<void>;
}
```

| 接口                                 | 请求/输出约束                                         | 失败处理                                |
| ------------------------------------ | ----------------------------------------------------- | --------------------------------------- |
| `GET /orders` 等查询                 | QuerySpec + 明确 page/cursor 方案；total 未知允许省略 | 请求取消、超时重试；不显示伪造总数      |
| `POST /orders`                       | 幂等键 + 已验证 schema；服务端返回规范化对象          | 422 映射字段；重复键返回原结果          |
| `PATCH /orders/:id`                  | expectedVersion/If-Match；权限在服务端检查            | 409 显示差异并要求重新确认              |
| `POST /approval-tasks/:id/decisions` | taskVersion、decision、意见、幂等键                   | 403 不可操作；410 已失效；409 重新加载  |
| `PUT /graphs/:id`                    | schemaVersion、expectedVersion、GraphDocument         | 校验错误定位 node/edge/property         |
| `POST /runs` 与事件订阅              | 配置版本、上下文引用、幂等键、事件游标                | 重连后按 eventId 去重；缺序列可重拉快照 |
| `POST /runs/:id/cancel`              | 请求取消与已取消结果区分                              | 无法撤销的外部动作展示实际/未知状态     |
| `POST /tool-calls/:id/approve`       | toolCallId、版本、动作摘要、过期时间、决定            | 修改动作后旧确认失效；不得静默扩大权限  |

流式协议可以映射到 SSE，`id`/重连相关行为参考 [MDN SSE 说明](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events)。采用 fetch 流时须自行实现中止/重连解析，不假定等同原生 EventSource。动态表单与工具参数可采用 [JSON Schema 2020-12](https://json-schema.org/draft/2020-12) 描述并独立验证，不能在 UI 中执行 schema 字符串脚本。

## 8. 工程验收、可观测性与发布

### 8.1 新增质量任务

| ID  | 优先级/大小 | 任务与产物                                           | 依赖            | 单项验收                                                          |
| --- | ----------- | ---------------------------------------------------- | --------------- | ----------------------------------------------------------------- |
| H01 | P0/M        | 覆盖脚本从 registry 自动发现路由、示例和必需状态     | A01,G11         | 新增组件没配演示则 CI 失败；重名/孤立路由检查                     |
| H02 | P1/M        | 关键流程 E2E：ERP/OA/Agent + 失败/权限/冲突 fixtures | G03,G05,G08     | 测试最终业务结果，避免只断言按钮存在                              |
| H03 | P1/M        | 视觉回归与键盘/读屏抽检矩阵                          | G11,A05         | 对照明暗/密度/视口；axe serious/critical 为零；焦点可见且顺序可用 |
| H04 | P2/M        | 性能基线：表格/图表/画布/消息长列表/内存             | A07,D11,E10,F06 | 原始测量和环境可复现；明确基线和回归预算                          |
| H05 | P1/M        | mock 异常控制台与确定性事件重放                      | G01,F01         | 可选择超时、乱序、重连、403/409/429、部分失败；一键恢复           |
| H06 | P1/M        | 发布渠道、预览、消费样例与版本回滚演练               | A03,H02         | main 与分支预览隔离；旧版本可重装；数据 schema 向后兼容策略可验证 |
| H07 | P2/M        | 多端运行验证：React/Vue2/小程序/Flutter 的关键交互   | A02,G11         | 区分源码生成、编译、真机/模拟器；缺验证不得标 runtime-verified    |
| H08 | P1/M        | 可观测性适配器与数据脱敏策略                         | F01,G01         | traceId 贯穿 UI 请求/事件/错误；默认不上传用户输入和文件内容      |

### 8.2 测试与指标口径

保留现有合入门槛：

```bash
npm ci
npm run check:parity
npm run typecheck
npm test
npm run build
npm run check:a11y
npm run check:responsive
npm run check:layout
npm run check:motion
npm run build:packages
```

本地失败不能通过降低现有阈值解决；对环境阻塞要记录原因，交由具备依赖的 CI 运行。UI 验收除截图还应点通主流程。视觉截图固定日期、数据种子、主题和字体，等字体与内容完成后再比较。

可访问性目标采用 WCAG 2.2 AA 中适用条款，交互模式参考 WAI-ARIA APG；自动扫描只覆盖其中一部分，仍需键盘与读屏人工验证。[WCAG 2.2](https://www.w3.org/TR/WCAG22/)、[WAI-ARIA APG](https://www.w3.org/WAI/ARIA/apg/)

| 指标           | 定义与目标                                                                              | 数据源/维度                                         |
| -------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------- |
| 能力与状态覆盖 | 见 6.2；implemented 能力可运行案例目标 100%                                             | registry + 测试报告；按端/分类/状态                 |
| 安装成功率     | 必测消费项目安装、类型、打包、运行均通过 / 必测项目总数，目标 100%                      | 干净 tarball 消费 CI；框架/版本/打包器              |
| 核心业务通过率 | 必测成功与异常用例通过数 / 必测数，发布目标 100%                                        | E2E；角色/网络/实体状态                             |
| 文档检索成功   | 固定查询集能定位目标组件的比例；建立基线后设目标                                        | 搜索 fixture；中文/英文/业务别名                    |
| 性能预算       | 初始候选：表格 1 万行、图表 1 万点、画布 500 节点/1000 边、消息 1000 条；按设备实测调整 | 基准脚本；p50/p95、首绘、交互延迟、内存、卸载后资源 |
| 包体积回归     | 同构建条件下 gzip 增量；常规回归预算建议 ≤5%，超出需说明与审批                          | build artifact；按入口/依赖/版本                    |
| AI 运行稳定性  | 已完成/失败/取消/等待确认分开计数；成功率不把取消和等待强行当失败                       | mock 重放/真实适配器；模型版本/工具/错误码          |
| AI 延迟与用量  | 首字时间＝首个有效正文片段时间－请求接受时间；总耗时＝终态－接受时间                    | run events；队列/工具/生成分别计时，缺失计 unknown  |
| 错误可定位性   | 每条非预期错误有 traceId、版本和错误码；不记录 secret/完整提示词                        | LoggerAdapter/Transport；默认本地，可注入外部系统   |

这些容量与预算是计划测试输入，不表示现有实现已达到。可用性优化应以测量为依据，不能用关闭动效、减少数据或取消状态测试掩盖缺陷。

### 8.3 灰度和回滚

1. 原子组件保持兼容；新 props 可选，现有默认行为保持，破坏性接口另立迁移任务。
2. 新组合/复杂图表/编辑器通过独立入口和 feature flag 引入；先在示例与 next 版本验证，再进入稳定版。
3. 发布前在干净 checkout 跑检查、pack 消费端、许可证和依赖审计；签入生成物，记录版本与构建 SHA。
4. 文档分支预览沿用现有 `.github/workflows/preview.yml`，main 部署沿用现有链路。示例应用子路径/静态资源路径一并验收。
5. 失败回滚到上一已验证版本；GraphDocument/示例本地存储升级先备份和迁移，旧版不能解释的新 schema 应显示明确提示，不能静默覆盖。
6. 自动发布到注册表、生产数据迁移、真实 AI 工具执行属于后续独立动作；本规划不代表已经执行。

## 9. 推荐实施批次与拆 PR 方法

批次的第一条规则：**第 5 节和第 8 节定义的任务，每一个都必须出现在某一批里**。
这份表是照着任务 ID 全集核过的，H 组的质量任务尤其容易被忘掉——它们不产出
可见界面，但 H01 是 P0。批次内部按第 5 节和第 8 节表里的依赖列排序；
高优先级任务不得排在它所依赖的任务之前（B13/B18/G11 的优先级已按此上调）。

### 第一批：建立可信基线

A01/A02/A04/A05/A08/F01；输出可追溯能力 registry、消费项目与事件协议。完成后可以回答“当前哪些东西确实可用”。A03/A06/A07 按风险继续，避免把所有 SSR/平台扩展都压进第一天。

### 第二批：一条业务链带动组合组件

B01/B02/B03/B04/B08/B09/B12、G01/G02/G03/G11、H05/H01。先让 ERP 订单查、新建、详情、状态变化、失败恢复跑通，再从稳定业务复用点抽取 Pro 组件。新增共享组件按 3.0 定下的 pro 层范围做端适配。
H01 紧跟 G11：覆盖脚本要靠 Patterns Lab 的注册表才能发现演示，G11 一合并就把 H01 接上，别让「新增组件没配演示」这条门槛空置到第五批。

### 第三批：审批和智能体闭环

G05/G08、H08；其依赖 B10/B13/B18/E01 ～ E08/F02 ～ F08/F10 分成独立 PR，顺序按表中依赖。
B13（Markdown/富文本）必须排在 F04（消息渲染）之前——F04 直接消费它；
B18（通知中心）必须排在 G05 之前——G05 的验收里就有「通知」这一步。先用确定性 mock 证明“请求 → 执行 → 确认 → 结果”，再接真实服务。

### 第四批：图表与业务广覆盖

D01 ～ D08/D10/D12/D13、C01 ～ C06、B05 ～ B07/B11/B14 ～ B17、E09/E10、G04/G06/G07/G09/G10、H02/H03。每完成一个图族或页面族，就登记 registry 的样式/状态用例与应用页面。
E09/E10 排在 D08 之后（E09 依赖它）；H02 要等 G03/G05/G08 三条业务链都在，H03 要等 G11 与 A05。
D13 是从 D03 里拆出来的：百分比堆叠、阶梯与目标线都是数据变换，四个端共用一份实现就够；
横条与双轴要动坐标轴本身（轴的方向、第二值轴、单位限制），四端各有一套绘制，
与前者混在一个任务里只会让「做完了吗」说不清。

### 第五批：规模与生态

D09/D11、F09/F11 ～ F15、G12、H04/H06/H07。以实际应用需求决定地图、金融图、语音、多端复杂编辑器的先后，不预设所有能力必须同时落地。
H06（发布渠道与回滚演练）依赖 A03 与 H02，放在这一批收口；它没做完之前不宣称「可发布」。

每个 L 任务建议拆成：① 契约/逻辑+测试，② 默认渲染与交互，③ 异常/无障碍/适配，④ 文档与消费端验证。拆分 PR 可暂存分支，但里程碑完成必须以完整验收为准。

建议下一次任务提示模板：

```text
从 main 最新代码新建分支，实现 docs/astra.md 的 [任务ID]。
先核对源码现状与依赖，禁止重复实现已有能力。
交付：共享逻辑/API、必要的各端适配、文档、真实业务用例、检查结果。
依照 CLAUDE.md 运行现有门槛；不得放宽 parity/a11y/responsive/motion。
完成后回填本任务 PR、验证证据和剩余边界，不勾选尚未实现部分。
```

## 10. 进度核对（截至 2026-09-14）

判定口径，三档，宁可判低不判高：

- **✅ 完成**：有实现；若是库组件则五端齐备且都从入口导出；有自动检查或测试守着；
  文档站有可运行演示。四条缺一条都不算完成。
- **🟡 部分**：核心能力在，但验收条件里有明确的一条还没做到——括号里写清缺什么。
- **⬜ 未开始**：没有对应实现。

「有源码」不等于完成，「构建通过」也不等于完成——这张表按上面的口径填，
不按工作量填。

### A. 基础治理

| ID | 状态 | 证据 |
| --- | --- | --- |
| A01 | ✅ | `scripts/build-capability-registry.mjs` + `check-capability-registry`，覆盖矩阵页 |
| A02 | ✅ | `scripts/check-install.mjs`：打包 → 仓库外四个消费项目 → 断言渲染出的实际色值 |
| A03 | ✅ | `VERSIONING.md`、`CHANGELOG.md`、`scripts/release.mjs`、`.github/workflows/release.yml` |
| A04 | ✅ | `README.md`、`CONTRIBUTING.md`（含「已验证 / 未验证运行时 / 未发布」矩阵） |
| A05 | ✅ | `scripts/check-style-rules.mjs`、`check-a11y`（零容忍）、`logic/palette.ts` 的 `solidPair` |
| A06 | ✅ | `scripts/check-ssr.mjs`：无 DOM 导入、Vue/React SSR、两档 CSP 下的 hydration |
| A07 | ✅ | `scripts/check-bundle.mjs`：按入口称重，单图标 gzip < 1 KB 是硬断言 |
| A08 | ✅ | `scripts/check-licenses.mjs`、`THIRD-PARTY.md`、`assets/PROVENANCE.json` |

### B. 原子组件补强与页面组合

| ID | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| B01 | ✅ | `IAppShell` 五端；示例层的壳已改为在它之上只加应用切换器与源码入口 |
| B02 | ✅ | `IPageState` + `logic/pagestate.ts`，七种状态的优先级有测试 |
| B03 | ✅ | `IQueryFilter` 五端 + `logic/query.ts` + 42 条 Dart 对齐断言 |
| B04 | ✅ | `IProTable` 五端 + `logic/protable.ts`（带序号的请求、过期响应丢弃并记账、总数与行同源） |
| B05 | ✅ | `logic/columns.ts`（显隐/顺序/宽度/固定/密度/视图持久化），隐藏与权限分开；小程序的固定列走「优先显示」替代路径 |
| B06 | 🟡 | `ITreeTable` 五端 + `logic/treetable.ts`（排序只在兄弟之间排、折叠不丢选择并数出「有几项在收起的分组里」、汇总按叶子算因而不受折叠影响、小计跟在分组最后一条子行之后），24 条 vitest + 70 条 Dart 对齐断言；多级表头与单元格合并还没做 |
| B07 | ✅ | `IBulkBar` 五端 + `logic/bulk.ts`（三种作用域各有摘要、「全部匹配」要再确认且不给 id 名单、部分失败只重试失败项、多轮重试成功集累加），81 条 Dart 对齐断言 |
| B08 | ✅ | `ISchemaForm` 五端 + `logic/schemaform.ts`（条件是数据、环检测、数组子表、服务端错误落位），40 条 Dart 对齐断言 |
| B09 | ✅ | `IFormPage` / `IDrawerForm` / `IModalForm` / `IStepForm` 五端 + `logic/formhost.ts`（重复提交、失败保留输入、离开保护、重置范围、分步不丢数据），97 条 Dart 对齐断言 |
| B10 | ✅ | `IEntityPicker` 五端 + `logic/entitypicker.ts`（已选存完整对象所以翻页不丢名字、不可选的留在列表里说原因、停用的不能新选但已选照常显示与移除），44 条 Dart 对齐断言 |
| B11 | ✅ | `IImportWizard` + `IExportJob` 五端 + `logic/importjob.ts` / `logic/exportjob.ts`（列映射可回退、预检不落库且在签名上就不可能落库、错误清单带原始行号、同文件同映射算同一个幂等键；导出排队可取消、总数未知不画进度条、过期给「重新生成」、出处与文件名带时间与筛选摘要），22 + 113 条 Dart 对齐断言 |
| B12 | ✅ | `IDetailPage` 五端 + `logic/detail.ts`（状态不允许的动作不出现、没权限的出现但停用并说明、失效时写动作停而只读动作照常、返回票据与上一条/下一条），93 条 Dart 对齐断言 |
| B13 | ✅ | `IMarkdown` + `logic/markdown.ts`（令牌树、危险 URL 拒绝）、`ICodeBlock`、附件预览 |
| B14 | 🟡 | `IComment` 在，缺未读定位与活动记录 |
| B15 | 🟡 | `ICalendar`、`IChartGantt` 在，缺看板与人员泳道的拖动 |
| B16 | ⬜ | 登录/设置/角色管理页面模式未做 |
| B17 | ⬜ | 公共网页模式未做 |
| B18 | 🟡 | `INotificationLayer`、`ICommandSearch` 在，缺异步任务中心与三者的整合 |

### C. 图标与资源

| ID | 状态 | 证据 |
| --- | --- | --- |
| C01 | ✅ | `icons/meta.ts` + `check-icons`（元数据、分类、别名唯一性） |
| C02 | ✅ | 87 个图标，按语义域分类 |
| C03 | ✅ | `icons/filled.ts` 的描边/填充协议，双色用同一色两档透明度 |
| C04 | ✅ | 图标浏览器页（检索、尺寸/线宽预览、复制与下载） |
| C05 | ✅ | `build-icon-paths.mjs` 的单图标导出 + `check-bundle` 的实测断言 |
| C06 | ✅ | `illustrationMeta` + `check:illustrations`（@2x 尺寸、四角透明、替代文本） |

### D. 图表与分析

| ID | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| D01 | ✅ | `contracts/chart.ts` + `logic/dataset.ts`（单位/时区/聚合/数据质量） |
| D02 | ✅ | `IChartFrame`：口径、数据表、下载，缺失值不补零 |
| D03 | ✅ | 百分比堆叠、阶梯线、目标线 |
| D13 | ✅ | 横条与双轴（`logic/axis.ts` 五端共用，双轴把不该用的情形印在图下） |
| D04 | ✅ | `IChartDistribution` 五端（直方/密度/小提琴/误差棒）+ `logic/stats.ts`；散点矩阵的布局逻辑在，组件留到 D05 一起做 |
| D05 | ⬜ | 构成/归因（旭日、Icicle、帕累托、留存）未做 |
| D06 | ✅ | `logic/linkage.ts`：统一 SelectionEvent、下钻压栈、循环终止 |
| D07 | ⬜ | 实时滑窗/事件标记/断流状态未做 |
| D08 | 🟡 | `IFlow` 与 `logic/graph.ts` 在，缺邻接矩阵与调用轨迹视图 |
| D09～D12 | ⬜ | 地图、金融图、跨图协作等未做 |

### E. 流程与可视化编辑

| ID | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| E01 | ✅ | `contracts/graph.ts` v2：往返无损、`extras` 保留、v1 迁移、拒绝过新版本 |
| E02 | ⬜ | NodeRegistry / 节点工具箱 / 属性检查器未做（依赖 B08） |
| E03 | ⬜ | 端口连线编辑与校验面板未做 |
| E04 | 🟡 | `logic/flow.ts` 有框选、批量移动、缩放与缩略图，缺事务化的撤销/重做与剪贴板 |
| E05～E08 | ⬜ | 布局导航、GraphStore、节点模板、运行态覆盖未做 |
| E09 | ⬜ | 状态机/血缘模板与差异查看未做 |
| E10 | 🟡 | 触摸与键盘在 `IFlow` 里，缺大图性能用例与导出验收 |

### F. AI 对话与智能体

| ID | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| F01 | ✅ | `contracts/run.ts` + `logic/run.ts`：seq 排序、重复丢弃、终态、取消 |
| F02 | ✅ | `logic/transport.ts`：mock 与流式、取消、重试、恢复游标 |
| F03 | ✅ | `IChatList` + `logic/chatlist.ts`（搜索、重命名、归档、恢复） |
| F04 | ✅ | `IMessageParts`、`IMarkdown`、`ICodeBlock`：多段混排与流式更新 |
| F05 | 🟡 | `IPromptInput` + `logic/mention.ts` 在，缺附件校验与草稿恢复 |
| F06 | ✅ | `IRunStatus` 五端 + `logic/lifecycle.ts`（排队/连接/断线重连/等人确认四态，终态压断线、断线压生成中），96 条 Dart 对齐断言 |
| F07 | ✅ | `IChatToolCall` + `IToolChips`：输入输出、耗时、错误 |
| F08 | ✅ | `IApprovalCard` 的 `approvalGate`：即将过期仍可拍板、过期给「重新发起」、版本失效给「查看新版本」，两者同时成立以版本失效为准；65 条 Dart 对齐断言 |
| F09～F15 | ⬜ | 分支比较、产物工作区、上下文管理、Agent 配置、RunExplorer、评测、多模态未做 |

### G. 示例应用

| ID | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| G01 | ✅ | `examples/shared`：固定种子、可控时钟、mock API、版本化存储、重放 |
| G02 | ✅ | 门户 + 共用壳 + 应用登记表 + `check:examples`（双向链接、无死路） |
| G03 | ✅ | ERP 第一切片：列表用查询层 + `IBulkBar`，新建用 `IFormPage`，详情用 `IDetailPage`；422/403/409/幂等四条分支都在真构建产物上走通 |
| G04～G10 | ⬜ | OA、Analytics、Agent Studio、移动工作台未做（门户里如实标为「规划中」） |
| G11 | ✅ | Patterns Lab 自动注册 + `check:lab`（每个组件必须真的渲染出来） |
| G12 | ⬜ | 页面模板导出未做 |

### H. 工程与质量

| ID | 状态 | 证据 |
| --- | --- | --- |
| H01 | ✅ | parity / a11y / responsive / motion / lab / bundle / install / ssr / icons / routes / release / mp / flutter / illustrations / capability / examples，每条新检查都带故障注入 |
| H05 | ✅ | 生成物漂移检查（CI 里 `git diff --exit-code`） |
| H08 | ✅ | 确定性 mock：同一 seed 出同一批数据，截图与 E2E 不漂 |
| H02～H04、H06、H07 | ⬜ | 端到端业务链回归、灰度与回滚演练、渠道发布演练未做（H06 没做完之前不宣称「可发布」） |

### 下一步的顺序

按依赖与阻塞面排，不按好做程度排：

1. ~~B08 SchemaForm~~ —— 已完成，B09/B10/B11 与 E02 的前置已解除。
2. ~~B04 + B05 ProTable~~ —— 已完成，B06/B07/B11 与 G03 的组件化前置已解除。
3. ~~B01 AppShell 库组件~~ —— 已完成（形态先在示例里跑过一轮再抽出来），B12/B16/B17 的前置已解除。
4. ~~D04 渲染~~ —— 已完成。
5. ~~F06 / F08 的缺口~~ —— 已完成，两处「状态有了但没说出来」都补上了。
6. ~~B09 表单页形态~~ —— 已完成，四种形态齐了。
7. ~~B07 批量操作栏~~ —— 已完成。
8. ~~B12 DetailPage~~ —— 已完成。
9. ~~G03 换成组件~~ —— 已完成，G03 转正。
10. ~~B10 人员 / 组织 / 资源选择~~ —— 已完成。
11. ~~B11 的另一半：ExportJob~~ —— 已完成，B11 整条转正。导出按「任务」做：
    排队可取消、总数未知时不画进度条（假进度条是谎）、过期给「重新生成」
    而不是一个坏链接、出处（时间 / 筛选 / 列 / 行数）与文件名一起交出去。
12. ~~B06 树表与分组汇总~~ —— 树表、分组小计与合计已完成（`ITreeTable` 五端）。
    「排序与展开不能丢选择」这条已经立住：排序只在兄弟之间排、折叠只动视图、
    表头复选框的口径是整棵树。**剩下多级表头与单元格合并**，
    它们是另一类问题（表头的结构，不是行的状态），排在 B14/B15/B18 之后。
13. **B14 / B15 / B18 的补完** —— 评论的未读定位、看板与人员泳道的拖动、
    异步任务中心；三条都是 🟡，各缺一块明确的东西。

## 10. 参考与本次视觉取舍

参考访问日期：2026-09-13。下列描述是对页面结构的观察与本项目取舍，不代表复用其代码或资产。

- [Turbo](https://turbodesign.co/)：较强的标题层级与大幅产品画面。用于决定展示真实产品情境与留白比例。
- [GetLayers](https://www.getlayers.ai/)：克制容器、清晰的内容分组和作品缩略预览。用于建立组件集合的层级与索引节奏。
- [Supahero](https://supahero.io/)：Hero 与作品陈列的直观浏览方式。用于建立首页的编辑式排版和分类展示。
- [i-design 当前站](https://ignorance-shiyao.github.io/i_design/)与[源码仓库](https://github.com/ignorance-shiyao/i_design)：保留品牌、语义令牌、导航与多端设计原则。

不将参考站的整页文案、商标、收费模板或插画带入本仓库。首页所有图形由现有组件、CSS 形态和已有小白/十五素材组成；业务预览按真实可操作范围表述。详细技术来源在对应章节紧邻说明，避免将建议写成未经核验的现状事实。
