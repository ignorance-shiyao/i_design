# 交接说明

给下一个接手这个仓库的人（包括 AI）。读完这一页就能开工，不必翻聊天记录。

主台账是 [`当前现状&后续规划_Claude .md`](./当前现状&后续规划_Claude%20.md)，
**那是唯一的任务来源**；本文只讲怎么干活、什么算干完。

---

## 一、先做这三件事

```bash
git clone https://github.com/ignorance-shiyao/i_design.git
cd i_design
npm ci                     # 必须用 ci，不是 install（见下文 .npmrc 那条）
npm run check:parity       # 基线：这一步在干净仓库上必须全绿
```

然后按顺序读：

1. **`CLAUDE.md`**（仓库根目录）——硬性规则。违反其中任何一条都算缺陷，
   即使构建通过、测试全绿。里面有几条是反直觉的（不用加粗边线表达状态、
   元素色不用渐变、深色底一律白字），先读完再动手。
2. **`docs/当前现状&后续规划_Claude .md`** 第 1 节「现状盘点」——知道现在有什么。
3. 同一份文档第 2 节「任务台账」——挑一个 `- [ ]` 的任务开工。
4. 本文第三节「什么算干完」——这是验收标准，比「写完代码」严格得多。

---

## 二、现在是什么状态

- 分支：`claude/bold-bohr-fl0nig`（已推送，与 main 的差异就是下面这些任务）
- 已完成 20 项，未完成 20 项，都在台账里逐条写着
- 覆盖矩阵 120 个组件，五端一致；golden 断言 1129 条；单元测试 38 条
- 包已经能装出去（`npm run build:packages`），CI 在 PR 上跑

**已知的坑，别重复踩：**

| 坑 | 说明 |
| --- | --- |
| `packages/vue`（Vue 2.7）的类型不能走 vue-tsc | vue-tsc 绑的是 Vue 3 语言服务，拿去编 2.7 会把合法产物报成错。类型由 `packages/vue/scripts/build-types.mjs` 按源码生成；解析器必须把 `Record<string, T>` 里的逗号当成泛型参数，否则 d.ts 会写成非法语法 |
| `.npmrc` 的 `legacy-peer-deps=true` | 不能删。删了 CI 的 `npm ci` 直接失败——Vue 2.7 与 Vue 3 共存是刻意的 |
| 不要用 `cat >` 覆盖已存在的文件 | 出过事故：覆盖 `logic/countdown.ts` 时把整个移动端实现冲掉了，而构建照样绿 |
| 生成物不能手改 | 清单见 `docs/HANDOVER.md` 第 3 节。手改的内容下一次构建就没了，且不会报错 |
| 文案不写死 | 所有用户看得见的固定字走 `logic/locale.ts`；组件自己传了优先于字典 |

---

## 三、什么算「干完」

**「构建通过」不等于「能用」。** 一个任务算完成，要同时满足：

1. **`npm run check:parity` 全绿**（它串了 17 项检查：跨端一致、生成物新鲜度、
   属性一致、小程序四件套、Flutter golden、文档快照……）
2. **`npm run typecheck`、`npm test`、`npm run build` 全绿**
3. **改了界面就用 Playwright 真跑一遍**，亮暗两态 + 390px 窄屏各看一眼。
   不是截图存档，是**量出来**：改了尺寸就量高度，改了动效就量过渡中的位置。
   这个项目里已经有两次「代码看着对、量一下差 4px」的例子
4. **改了依赖就在 `git archive` 出的干净树里验证**
5. **新增组件要五端齐备**（Vue 3 / Vue 2 / React / 小程序 / Flutter），
   覆盖矩阵不得回退
6. **新增共享逻辑要补 golden 断言与单元测试**，两者都不能少：
   golden 保证「各端算得一样」，单元测试保证「算得对」
7. **台账勾掉对应任务**，并在第 1 节现状里改一句话（F2）

### 浏览器验证怎么跑

```bash
npm run dev -- --port 5199          # 另开一个终端
# 站点是 hash 路由：http://localhost:5199/#/components/button
```

Playwright 用仓库里已装好的：`chromium.launch({ executablePath: '/opt/pw-browsers/chromium' })`
（自己的机器上就用默认的）。临时脚本放临时目录，不要进仓库——
`.gitignore` 已经挡了 `*.tmp.mjs` / `*.tmp.png`。

---

## 四、加一个组件的完整流程

台账第 5 节有一份更细的，这里是最短路径：

1. `src/components/IXxx.vue` —— Vue 3 是**源**，vue-next 与 vue2 由脚本生成
2. `src/components/index.ts` 加导出
3. `packages/react/src/components/Xxx.tsx` —— React 手写（index 由脚本生成）
4. `packages/miniprogram/src/components/xxx/{index.js,index.wxml}` ——
   另外两个文件由 scaffold 生成
5. `packages/flutter/lib/src/components/i_xxx.dart`
6. 样式写一份：`packages/common/src/styles/components/xxx.css`（各端共享）
7. 纯逻辑抽到 `packages/common/src/logic/xxx.ts` + Dart 镜像 + golden 断言
8. 文档页 `src/pages/components/XxxPage.vue` + 路由 + `src/data/components.ts`
9. `npm run check:parity`——它会告诉你还缺哪一端

---

## 五、怎么交代给另一个 AI

把下面这段原样发给它就行：

> 仓库：https://github.com/ignorance-shiyao/i_design
> 分支：`claude/bold-bohr-fl0nig`
>
> 先读 `CLAUDE.md`（硬性规则）、`docs/交接说明.md`（怎么干活、什么算干完），
> 再从 `docs/当前现状&后续规划_Claude .md` 的任务台账里挑一个 `- [ ]` 的任务。
> 推荐顺序：B1/B3/B4/B5 → C 组。
>
> 每个任务做完必须满足交接说明第三节的七条验收，尤其是：
> 改了界面要用 Playwright 真跑并**量**出来，不是只截图；
> 新增共享逻辑要同时补 golden 断言与单元测试；
> 做完把台账里对应任务勾掉。
>
> 一个任务一个 commit，提交信息写清楚**为什么这么改**，
> 而不是复述改了哪几个文件。

### 建议的下一批任务

| 优先级 | 任务 | 为什么是它 |
| --- | --- | --- |
| P1 | **B1 / B3 / B4 / B5** AI 交互四项 | 这是这套库的差异点，缺口也最大。B5（代码块）最容易：站点里 `src/site/CodeBlock.vue` 已有实现，抽成组件即可 |
| P2 | **E10** 清理无障碍存量 | 有基线文件兜着，改一批缩一批，进度可见 |
| P2 | **C 组** 流程图四项 | 对齐辅助线 / 锚点选择 / 分组 / 自动布局，彼此独立，适合并行 |

**不建议**上来就做 A7（React 列表补位）——它要手写一遍 FLIP，收益不如上面几项。

---

## 六、评审这件事

如果你是被叫来**审别人的活**的，按这个顺序：

1. 先独立验证，别信 PR 描述：把分支拉下来自己跑第三节那七条
2. 故障注入：把它新加的检查故意弄坏一处，看是不是真的会红。
   这个项目里出现过「检查加了但永远不会失败」的情况
3. 读它改的生成物：生成物与源码对不上，说明它手改了生成物
4. 看它有没有在台账里勾掉任务并更新现状——漏了这步，
   下一个人会重复做一遍
