# 版本、弃用与兼容策略

## 一起发版，共用一个版本号

各端的包**同版本号一起发**：`@i-design/vue-next@0.3.1` 与
`@i-design/react@0.3.1` 是同一套决策的两个渲染适配，
令牌、图标与交互规则来自同一份 `@i-design/common@0.3.1`。

分开编号会立刻带来一个没人答得上来的问题：
「我装的 React 0.4 和 common 0.2 是配套的吗」。
所以包与包之间的依赖一律写死同一个版本号，不用 `^`。
`npm run check:release` 会核对这件事，Flutter 的 `pubspec.yaml` 也一并核。

## 版本号怎么走

0.x 阶段按 `0.次版本.修订` 走：

| 改动 | 版本 |
| --- | --- |
| 修缺陷、补文档、性能 | 修订（0.3.0 → 0.3.1） |
| 新组件、新属性、新令牌（现有用法不变） | 次版本（0.3.1 → 0.4.0） |
| 破坏性变更 | 次版本，且必须带迁移说明 |

到 1.0 之后改为标准 semver：破坏性变更走主版本。
「0.x 可以随便破坏」这条惯例这里不采用——使用方的项目不会因为我们还没到 1.0
就少坏一次。

## 破坏性变更

一条破坏性变更进 CHANGELOG 时必须同时给出三样，缺一样不算完成：

1. **变了什么**：旧写法与新写法各一段代码；
2. **为什么**：不是「更规范」，而是旧写法在什么情况下会出错；
3. **怎么改**：能机械替换的给出替换规则；不能的说明要人工判断哪些地方。

## 弃用

弃用不是删除。一个属性、组件或令牌被弃用时：

- 保留至少一个次版本，期间照常工作；
- 在类型上标 `@deprecated`，注释里写清替代品；
- 在 CHANGELOG 的「弃用」一节登记，写明**计划在哪个版本移除**；
- 到了那个版本才真正删掉，并作为破坏性变更记一笔。

没有写移除版本的弃用条目会让 `npm run check:release` 失败——
「先标着以后再说」的弃用会一直标着。

## 预发布通道

不确定的改动先发到 `next` 通道：

```bash
npm run release -- --tag next --dry-run   # 先看会发出去什么
npm run release -- --tag next             # 真发（需要 NPM_TOKEN）
```

`next` 上的版本号带预发布后缀（`0.4.0-next.1`）。
装 `@i-design/vue-next` 不带 tag 的使用方拿不到它——
`latest` 只在这套东西在示例应用里跑通之后才动。

## 发布的前提

- 发布只从 CI 的手动流程走，`NPM_TOKEN` 只存在 CI 的 secret 里，不在任何人的机器上；
- 发布前跑完整门槛，并在干净 checkout 里验证一次；
- 已经发出去的版本不撤回、不覆盖：错了就发下一个修订版。
  npm 的 unpublish 会让所有装了它的项目在下一次安装时失败。
