<view
  class="i-col {{offset > 0 ? 'is-offset' : ''}} {{keep ? 'is-keep' : ''}} {{flex ? 'is-flex' : ''}}"
  style="--i-col-span:{{span}};--i-col-span-sm:{{sm}};--i-col-offset:{{offset}}"
>
  <slot />
</view>
