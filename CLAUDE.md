# i_design 项目约定

本文件是这个仓库的硬性规则。**违反其中任何一条都算缺陷**，即使构建通过、测试全绿。

## 视觉禁令

### 禁止用加粗/加深的边线表达状态或类型

**绝对禁止**这类写法：

```css
/* 禁止 */
border-left: 3px solid var(--i-color-brand);
border-left: 4px solid var(--i-color-danger);
border-top: 3px solid …;   /* 任何方向都一样 */
```

即用一条比其余边框更粗、颜色更重的边线，去标记「这是什么类型」「这是什么状态」。
不论出现在提示条、卡片、列表项、附件条、标签页还是任何别处，一律不用。

**替代做法**：状态与类型用**图标 + 淡底色块**表达，边框保持四边等宽的发丝线：

```css
/* 正确 */
border: 1px solid var(--i-color-hairline);

.x__icon {
  color: var(--i-color-danger);
  background: color-mix(in srgb, var(--i-color-danger) 12%, transparent);
}
```

并且**颜色不能是唯一线索**——同时给出文字标签（「图片」「错误」），
色觉障碍用户与灰度打印才读得出来。

例外：用 `border` 拼几何形状（例如表格排序的小三角，靠
`border-left: 4px solid transparent` 构成三角形）不属于此列，那不是边线样式。

### 元素颜色一律纯色，不用渐变

按钮、徽标、进度条、日期选中态、卡片底色等**元素的填充色必须是纯色**：

```css
/* 禁止 */
background: var(--i-gradient-brand);
background: linear-gradient(135deg, #6d8bff, #4a63c4);

/* 正确 */
background: var(--i-color-brand);
```

渐变会让同一个主题色在不同尺寸的元素上呈现出不同的观感，
主题配置换色后也更难保证对比度。

例外：**纹理与氛围底**不属于元素色，可以继续用渐变——
点阵网格（`radial-gradient` 画点）、页面顶部的光晕、遮罩
（`mask-image`）、骨架屏的扫光。它们描述的是背景质感，不是某个控件的颜色。

### 实心色块上的字：底色与字色成对取，不是一律白字

饱和底色上的文字**先试着把底色压深一点点再用白字，压不动就换深字**。
判定规则在 `logic/palette.ts` 的 `solidPair`，令牌是成对的
`--i-color-X-solid` 与 `--i-color-on-X`：

```css
/* 正确：底色与字色成对 */
background: var(--i-color-brand-solid);
color: var(--i-color-on-brand);

/* 禁止：写死白字 */
background: var(--i-color-danger);
color: #fff;
```

为什么不是「一律白字」——四个语义色上的白字实际是：

| 底色 | 白字 | 深字 |
| --- | --- | --- |
| 品牌蓝 `#5e7ce0` | 3.86 | 4.18 |
| 危险红 `#f66f6a` | 2.84 | 5.68 |
| 成功绿 `#3ac295` | 2.25 | 7.17 |
| 警告橙 `#fa9841` | 2.18 | 7.40 |

后三个上的白字连图形的 3:1 下限都到不了。那不是「白字更好看」，那是字没印上去。

`solidPair` 的两步与那个**预算**是关键：品牌蓝只要压深 ΔL 0.04 就够（`#5370d2`），
压完还是同一个蓝，识别色不受影响；成功绿与警告橙要压掉 0.19 才轮得到白字，
那时它们已经是墨绿和棕色，不再是这套体系的颜色了——所以它们换深字。
没有预算的话，算法会为了凑够对比度把橙色一路压成棕色。

深字不用纯黑，而是带着同一色相的近黑：纯黑压在彩色块上显得脏。

主题面板换色时这一对跟着一起推导，因此换成明黄那类浅主题时字会自动转深，
而不是硬顶着白字。图表的分类色与色阶同理，用 `--i-chart-N-ink` /
`--i-chart-seq-N-ink`——那里预算为 0：色阶编码的是量级、分类色是身份，
压深任何一档都等于改了数据的含义。

`npm run check:a11y` 是零容忍的：任何一条 serious / critical 都算失败。
别去加「先记下来以后再说」的清单——那样这个检查很快就又变回「反正是红的」。

### 正文不许通栏排，一行最多 45 个汉字

段落不加宽度上限，在 1440 的桌面上就会一行排到底。文档站内容区有 1000px 宽，
量出来是一行 71 个汉字——读到行尾再回到下一行行首，眼睛要横扫整整 1000px，
很容易串行。这就是「页面看着满当当，却读不下去」的来源。

```css
/* 正确 */
max-width: var(--doc-measure);   /* 文档站，45em */
max-width: 45em;                 /* 组件库里的长正文 */
```

上限一律写 `em` 不写 `ch`：`ch` 是数字「0」的宽度，随字体变，同样的 `90ch`
在两套界面字体下量出来差着七个汉字。一个汉字恰好是 1em，`45em` 就是 45 个汉字。

只封顶，不撑宽——窄容器里这条规则什么也不做，所以给它加上永远不会让手机变糟。

例外：代码块、表格、SVG 里的文字不受此限。它们的宽度由内容决定，截断只会逼出
横向滚动。

### 卡片网格里的格子按内容定高，不许被同行最高的那个撑起来

网格默认 `align-items: stretch`：一行里所有格子被最高的那个拉齐，
拉出来的高度全灌进卡片内部，成片的灰底或白底。Patterns Lab 出过这个问题——
18% 的网格高度是撑出来的，最惨一格被拉高 224px，滚过去看到的大半不是组件。

```css
/* 不够：行高仍由该行最高的格子决定，空白只是从格子里挪到了格子之间，页面一样长 */
align-items: start;

/* 正确：高矮差得远的清单用多列 */
column-width: 320px;
column-gap: var(--i-spacing-4);
/* 子项上 */
break-inside: avoid;
```

多列的代价是阅读顺序变成先下后右，所以它只适合本来就没有横向语义的清单
（按名字排的组件清单是，一张对照表不是）。DOM 顺序不要动：键盘 Tab 与读屏
走的必须还是同一条路。

一排卡片为了对齐差个二三十像素是正常的版面处理，不在此列。

例外：**格子里的空白本身就是可交互的落点**时（看板的列、日历的格），
拉齐是功能而不是浪费——把一列压到内容高度，一个空列就没地方放卡片了。
这种网格在元素上写 `data-stretch-reason="…"` 说明理由即可豁免，
但**理由必须写出来**：`check:layout` 会把每一条豁免连同理由打进报告，
留空的不算豁免。一个不用写理由的开关，半年之后会长在每一个被这条拦下的网格上。

### 这两条只在宽屏上暴露，手机上一点痕迹都没有

`check:responsive` 跑的是 390 / 320，那里一行只有十几个字、一行只有一格，
所以它在上面两件事最严重的时候一直是绿的。量它们的是
`npm run check:layout`（1440px，量渲染后的结果而不是读 CSS 文本）。
**改了界面两条都要跑**，只跑窄屏的那条不算量过。

### 图标不用 emoji

图标一律走 `packages/common/src/icons/index.ts` 的 SVG path，小程序端用 CSS mask
跟随 `currentColor`。任何位置都不得用 emoji 充当图标。

## 架构约定

- **令牌、图标、纯逻辑只存在一份**，放在 `packages/common`；各端只写渲染适配。
- 组件样式**只引用语义令牌**（`var(--i-color-*)` / `var(--i-radius-*)`），
  不写死色值与尺寸——否则主题配置面板一调就会露馅。
- **pro 层（`packages/pro-*/`：ProTable / QueryFilter / SchemaForm 这类组合件）
  与原子组件一样是全端的**，进覆盖矩阵。某个端确实做不到的，写进
  `packages/common/src/contracts/capability.ts` 并指名替代路径——
  「该端暂不支持」不算声明，`npm run check:capability` 会把这种空话判红；
  声明写着不支持而实现还在，同样判红（过期的声明比没有声明更糟）。
- 「清单类」文件一律按目录生成（导出清单、覆盖矩阵、小程序四件套），
  手写必然会漏，且漏掉不会让构建失败。哪些文件是生成的，看 `package.json`
  里 `build:*` 那几条脚本各自写到哪儿。

## 交付约定

- **「构建通过」不等于「能用」**：改了界面就用 Playwright 真跑一遍看截图；
  改了依赖就在 `git archive` 出的干净树里验证。
- 新增组件要各端齐备（Vue3 / Vue2 / React / 小程序 / Flutter），
  跑 `npm run check:parity`，覆盖矩阵不得回退。
- 文档站文案从**产品自身视角**写：不写「对标某某库」、不写「抄自哪里」。
- **不强调数量**：不写「七端」「五端」「已实现 33 个组件」这类计数，
  说「多端」「各端」即可。读者关心的是「我的技术栈支持吗」，不是总数。
  覆盖矩阵页面里的具体数字是查询结果，不算宣传语，可以保留。

改动的验收标准：`npm run check:parity`、`typecheck`、`test`、`build` 全绿，
外加四项要真浏览器的——`check:a11y`（零容忍）、`check:responsive`
（390 / 320 两个宽度不许横向溢出）、`check:layout`（1440 宽度：正文行长与
卡片网格撑出来的空白）、`check:motion`（系统的减少动效与主题面板开关都要
真的把动效关掉）。改了界面就用 Playwright 真跑一遍并**量**出来。

再加两项**出了这个仓库才会暴露**的，改了组件、包入口或公共层导出就要跑：

- `check:bundle`——按需引入的隔离与体积。摇树失效不会让任何构建变红：
  只引一个按钮的产物悄悄变大，本仓库里一切照常，付首屏的是使用方。
  典型坏法是在模块顶层调函数（`export const x = build(TABLE)`），
  打包器证明不了它无副作用，于是那张表被钉进每一个碰到 barrel 的产物；
  确认纯的话加 `/* @__PURE__ */`，不要去放宽预算。
- `check:install`——把包 pack 出来装进仓库外的干净项目，真打包并渲染一次。
  仓库内的 alias 与 tsconfig paths 让一切看起来正常，装到别人项目里才炸；
  生成的 `.d.ts` 只要有一处语法错，使用方的 `tsc` 就整份解析失败。

这两条跑得久，但它们抓的恰恰是**静态检查全绿而东西已经坏了**的那一类——
和上面四条浏览器门禁同源。CI 是串行的，前面任何一步红，它们就根本跑不到，
所以「CI 之前没报过」不构成它们没问题的证据。

守卫要挡的是一类坏法，不是上次那一个。只堵住上次踩的那个坑，同类的下一个
照样过——挡「未闭合的 `Record<…>`」挡不住「少了分号的嵌套对象」，
要挡的其实是「生成物不是合法 TypeScript」。

所以补检查时顺手注入一次故障，确认它真会报红。仓库里的约定是
`check-X.mjs --faults` 配一个 `check-X.test.mjs`，后者 spawn 前者并断言
每条变异都被击中；照着现成的写（`check-adjacency` 是个短样本）。
一个从不触发的守卫，和没有守卫一样，而且更让人放心。
